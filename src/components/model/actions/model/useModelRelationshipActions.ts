/**
 * Relationship-oriented actions.
 *
 * This hook is currently a placeholder: most relationship actions live in diagram editing
 * and store mutations, not in the model-level action menu.
 */
export function useModelRelationshipActions() {
  return {};
}
